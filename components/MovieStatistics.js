import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const MovieStatistics = ({popularity, vote_count, release_date}) => {
  return (
    <View>
      <Text />
    </View>
  );
};

const styles = StyleSheet.create({});

export default MovieStatistics;
